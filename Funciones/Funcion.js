//FUNCIONES

function calcularCuadrado(numero) {
    const resultado = numero * numero;
    return resultado;
  }
  
  // Ejemplo de uso
  const numeroUsuario = 5;
  const cuadrado = calcularCuadrado(numeroUsuario);
  console.log(`El cuadrado de ${numeroUsuario} es ${cuadrado}`);
  